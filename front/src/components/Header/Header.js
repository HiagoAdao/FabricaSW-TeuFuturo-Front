import React from 'react';
import { HeaderContainer } from './Header.elements';

const Header = () => {
    return (
        <HeaderContainer>
            Header
        </HeaderContainer>
    )
}

export default Header
